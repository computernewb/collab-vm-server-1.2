Config:
- add TOML backend
    TOML store should be like:

    [collab3.host]
    listen_at = "127.0.0.1"
    listen_port = 2000

    which will write to the namespace collab3.host
    (collab3 being a namespace in the global namespace)

bespoke boost HTTP wrapper:
- rewrite ALL code to use coroutines, to simplify it
- refactor server code
- Add a router layer, because I really need one
- Allow buffer generator (it was merged in)
? Move websocket code to another partition, for organization

WebRTC:
- Implement bits and pieces of the protocols of the stack
- implement ASIO library for everything
- ...
- ???